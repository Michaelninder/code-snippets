num = input('Enter a number to get their PrimeFactors: ');
primeList = getPrimeList(num); # getPrimeList() returns all PrimeNumbers taht are smaller than root(num) ...
numPrimeFactors = [];
PrimeNumberIndex = 0;

def getPrimeFactors()
    CurrentPrimeNumber = numPrimeFactors[PrimeNumberIndex]
    while num > 1
        if num %%